const types = require("./schema/type/index");
const queries = require("./schema/query/index");
const mutations = require("./schema/mutation/index");
const resolvers = require("./schema/resolver/index");

module.exports = { types, queries, mutations, resolvers };
